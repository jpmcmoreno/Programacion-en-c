#include <stdio.h>

int main(void) {
  char nombre[] = "Juan"; //para cadenas de texto.

  int edad = 19;//para numeros enteros.

  float estatura = 1.65;//para numeros con decimal.

  char inicial = "J"; //para un unico caracter
  
  int arr[4] = { 10, 20, 30, 40 }; //para hacer un arreglo de datos.

  return 0;
}